---
title: Installation
description: Install Typewriting Class and start styling.
sidebar:
  order: 1
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

## 1. Install

<Tabs syncKey="pkg">
  <TabItem label="pnpm">
    ```bash
    pnpm add typewritingclass
    pnpm add -D typewritingclass-compiler
    ```
  </TabItem>
  <TabItem label="bun">
    ```bash
    bun add typewritingclass
    bun add -D typewritingclass-compiler
    ```
  </TabItem>
  <TabItem label="npm">
    ```bash
    npm install typewritingclass
    npm install -D typewritingclass-compiler
    ```
  </TabItem>
</Tabs>

## 2. Add the Vite plugin

```ts
// vite.config.ts
import { defineConfig } from 'vite'
import twcPlugin from 'typewritingclass-compiler'

export default defineConfig({
  plugins: [twcPlugin()],
})
```

The plugin handles CSS extraction and injection automatically. No manual imports needed.

For other bundlers see the [esbuild plugin](/compiler/esbuild/) or [Babel plugin](/compiler/babel/) docs. For Next.js see the [Next.js integration guide](/integrations/nextjs/).

## 3. Import the reset (optional)

```ts
// src/main.ts (or main.tsx, index.tsx, etc.)
import 'typewritingclass/preflight.css'
```

## 4. Style

```tsx
import { tw } from 'typewritingclass'

function App() {
  return <div className={tw.p(6).bg.blue500.textColor.white.rounded.lg}>It works!</div>
}
```

Start your dev server. You should see a blue rounded box with white text.

## Next steps

- [Quick Start](/getting-started/quick-start) -- build your first component
- [Editor Setup](/getting-started/editor-setup) -- VS Code hover previews
