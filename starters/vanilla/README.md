# typewritingclass Vanilla Starter

Minimal vanilla TypeScript + typewritingclass project template. No framework — just the DOM and typewritingclass.

## Quick start

```bash
# Copy this starter
npx degit typewritingclass/typewritingclass/starters/vanilla my-app
cd my-app

# Install dependencies
pnpm install

# Start dev server
pnpm dev
```

## What's included

- Vanilla TypeScript + Vite 6
- typewritingclass
- typewritingclass-compiler (Vite plugin, strict mode off)
- TypeScript 5.7
- Example `main.ts` with a hover card demo

## Scripts

| Command | Description |
|---|---|
| `pnpm dev` | Start development server |
| `pnpm build` | Production build |
| `pnpm preview` | Preview production build |
